type User {
  name: String!
  email: String!
  role: String!
}

type UpdatedUser {
  id: ID!
  name: String
  email: String
  role: String
  password: String
}

type AllUser {
  count: Int!
  records: [User]
}
